export enum FrequencyFormData {
  timesPerDay = 0,
  mealTime = 1,
  specificTimes = 2
}

export enum DailyFrequencyFormData {
  everyday = 0,
  specificDays = 1,
}

export enum DurationFormData {
  duration = 0,
  period = 1,
  untilNext = 2
}
